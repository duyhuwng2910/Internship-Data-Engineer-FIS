namespace DW_Test.Rpc.student
{
    public class TestStudentRoute : Root
    {
        private const string Default = Rpc + Module + "student-report";

        public const string Init = Default + "/init";
    }
}
