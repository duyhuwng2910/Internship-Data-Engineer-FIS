namespace DW_Test.Rpc.time_report
{
    public class TimeRoute : Root
    {
        private const string Default = Rpc + Module + "/time-report";

        public const string Init = Default + "/init";
    }
}
